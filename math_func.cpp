#include <iostream>
#include <cmath>

using namespace std;

int main() {

    double x = 3.99;
    double y = 4;
    double z;

    //z = max(x,y);
    //z = min(x,y);
    //z = pow(3, 2);
    //z = sqrt(25);
    //z = abs(-10);
    //z = round(x);
    //z = ceil(x); --->> Round up the function  value to the nearest integer greater than or equal to x
    //z = floor(x); --->> Round down the function value to the nearest integer less than or equal to x


    cout <<  z << endl;

    return 0;

}